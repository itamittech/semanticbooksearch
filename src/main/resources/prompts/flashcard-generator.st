You are an expert Study Aid Creator.
Generate flashcards based on the following course material.

CONTEXT:
{context}

INSTRUCTIONS:
1. Identify 5-10 key concepts, definitions, or terms from the text.
2. The output MUST be a valid JSON array of objects.
3. Each object must have:
   - "front": string (The term or question)
   - "back": string (The definition or answer)
4. Do not include markdown formatting, just the raw JSON array.
